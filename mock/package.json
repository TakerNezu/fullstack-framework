{
  "name": "mock",
  "version": "1.0.0",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "esbuild front/frontServer.ts --outfile=front/frontServer.js --platform=node",
    "dev": "yarn run mock:build && node front/frontServer.js"
  }
}
