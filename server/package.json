{
  "name": "server",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "build": "esbuild server.ts --outfile=server.js --bundle --platform=node",
    "start": "node index.js"
  },
  "dependencies": {
    "fastify": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^16.11.12"
  }
}
