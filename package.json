{
  "name": "fullstack-framework",
  "version": "1.0.0",
  "repository": "https://github.com/TakerNezu/fullstack-framework.git",
  "author": "Takeru Nezu",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "server",
    "front"
  ],
  "scripts": {
    "server:build": "esbuild server/server.ts --outfile=server/server.js --bundle --platform=node",
    "server:run": "yarn run server:build && node server/server.js"
  },
  "devDependencies": {
    "dotenv": "^10.0.0",
    "esbuild": "^0.14.2",
    "typescript": "^4.5.3"
  }
}
